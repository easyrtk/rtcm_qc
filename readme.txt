This simple utility is used to detect data gap (also message type) gap form logged RTCM data stream. Only RTCM MSM are supported. It is a command-line based with output at the screen. Use pipline to save the screen to a file.